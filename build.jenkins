#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent {
    label 'master'
           }

    stages {
        stage("create docker image") {
            steps {
                echo "==================== start build image ========================"
                dir ('Docker'){
                    sh 'docker build . '
                }
            }
        }
    }
}
